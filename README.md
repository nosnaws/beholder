# Beholder

An Elixir based Discord bot for sending notifications to a channel when someone enters a voice chat.
It uses the Nostrum library for interacting with the Discord API. Nostrum docs are available [here](https://kraigie.github.io/nostrum)

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed
by adding `beholder` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:beholder, "~> 0.1.0"}
  ]
end
```

Documentation can be generated with [ExDoc](https://github.com/elixir-lang/ex_doc)
and published on [HexDocs](https://hexdocs.pm). Once published, the docs can
be found at <https://hexdocs.pm/beholder>.

